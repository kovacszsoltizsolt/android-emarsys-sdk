buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath "org.ajoberstar.grgit:grgit-gradle:4.0.0-rc.1"
    }
}

import org.ajoberstar.grgit.Grgit

ext {
    git = Grgit.open(currentDir: projectDir)

    git.fetch()

    gitVersionName = git.describe()
    // 1602845230 is the date of 2020.10.16.
    gitVersionCode = (int) ((new Date().getTime() - 1602845230) / 10000)
    gitVersionCodeTime = git.head().time

    if (git.describe() == null) {
        throw new RuntimeException("Couldn't get Version Name")
    }
}

task printVersion() {
    println("Version Name: $gitVersionName")
    println("Version Code: $gitVersionCode")
    println("Version Code Time: $gitVersionCodeTime")
}